# Load the standard configuration for OCaml.
open build/OCaml
# open build/C
# open build/LaTeX

# The command-line variables are defined *after* the
# standard configuration has been loaded.
DefineCommandVars()

# Include the OMakefile in this directory.
.SUBDIRS: .